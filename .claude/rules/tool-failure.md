# Tool Failures

When a tool fails, use the [canonical failure rule](../../codex-rules/rules/tool-failure.md). Re-read changed text before retrying a failed exact-match edit; use the tool's actual parameter schema.
