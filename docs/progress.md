# Project Progress

English | [Chinese](progress-zh.md)

This file is the project-progress source of truth for __PROJECT_NAME__. It records completed work and remaining items for each task in reverse chronological order and must be updated whenever a task finishes or is interrupted.

Entry format: `timestamp / topic / completed work / remaining work`. Since 2026-07-14, an Agent must not make an unconfirmed decision for the user. When uncertainty remains, it must state known facts, options, and effects and ask the user to decide before implementing anything that depends on that decision. Historical entries labeled "Decision I made for you (can be overridden)" remain as records of the behavior at that time and are not the current working method.

Append new tasks below in reverse chronological order.

## 2026-08-31 CST / Complementary Archify and PlantUML diagram system

- Decision: replaced the former “Archify replaces PlantUML” rule with an explicit complementary matrix. Archify owns polished interactive overviews, complex workflows/data flows/lifecycles, themes, search, route tracing, and canonical PNG export. PlantUML owns Markdown-inline, diff-friendly ERD/class, precise state, and focused sequence/activity diagrams plus fast batch compilation. One illustration may have only one editable source.
- Implementation: added the project `plantuml-in-markdown` Skill and Chinese reference, a secure Markdown extractor/compiler, atomic SVG renderer, committed smoke fixture, `check:plantuml` / `gen:plantuml`, Archify-specific command aliases, combined diagram commands, and a CI job that downloads PlantUML 1.2026.1 only after SHA-256 verification. PlantUML forbids include/import directives and does not impose non-portable SVG byte freshness.
- Archify coexistence: updated the vendored Skill to route explicit PlantUML and inline-fence requests to the complementary workflow, recorded the fourth project-local modification, refreshed the vendored-tree digest, and kept all seven existing Archify diagrams under their 9/9, browser-review, and canonical PNG contract.
- Verification: the PlantUML fixture completed extract → compile → write back → full recompile, produced a non-empty 116×290 PNG and generated SVG, and passed human readability review. `check:plantuml` passed; the combined `check:diagrams` passed with seven Archify showcase 9/9 sources plus one securely compiled PlantUML block. The repository workflow passed actionlint v1.7.12, and the full `npm run quality` baseline passed outside the managed sandbox (the sandbox blocks nested Node fixture processes with EPERM).
- CI follow-up: the first pushed run failed only because CI could see Chinese text in tracked `.agents/skills/archify/agents/openai.yaml` while the hosted Codex mount hid that path from the local scan. The metadata is now English, and `check:localization` reads staged `.agents` bytes from the Git index whenever the working-tree mount hides them, closing the local/CI visibility gap.
- Data and external services: no runtime service, telemetry, user-data collection, or npm dependency was added. CI adds Java setup and a checksum-verified download of the pinned official PlantUML JAR; diagram compilation itself is offline and self-contained.

## 2026-08-31 CST / English-native repository documentation with maintained Chinese translations

- Documentation: preserved existing Chinese project Markdown as maintained `-zh.md` translations, while canonical paths now use natural English. Root entry points, `docs/`, Codex rules, Claude rules and Skills, contribution material, and the PR template all have reciprocal language links. Automatically loaded Claude translations live under `.claude/rules-zh/`; the two documentation indexes list only their own language.
- Code and contracts: converted first-party comments, docstrings, prompts, diagnostics, fixtures, development scripts, and public UI copy to English. The commit hook and Release Please title contract now require English Conventional Commit subjects. Added `check:localization` to reject unapproved Han on default-English surfaces and require synchronized document pairs, while explicitly preserving Chinese locale fixtures, contract terms, and paired initializer output as functional data.
- Diagrams and UI: rewrote all seven Archify sources in English with `meta.locale: "en"`, regenerated fresh interactive HTML and Viewer-native canonical PNG files, and reviewed every final PNG plus light/dark browser evidence. A true 390×844 render exposed mobile overflow from the longer English path; responsive wrapping fixed it, and final Chrome metrics were 390×844 and 1440×900 with `scrollWidth` equal to the viewport.
- Verification: `npm run quality` passed outside the managed sandbox; the sandbox-only run reached CI/CD fixtures before nested Node execution was denied with EPERM. `npm run check:archify` passed seven sources at showcase 9/9 with fresh HTML and valid native PNG dimensions. Documentation, localization, portable-document, contract, secret, site, CI/CD, syntax, hook, Python, line-ending, and real-browser checks all passed.
- Data and external services: this localization adds no user-data collection, telemetry, runtime third-party service, npm dependency, or network dependency.

## 2026-08-31 CST / Portable single-file export for Markdown with local illustrations

- Problem: repository Markdown references `docs/diagrams/*.archify.png`, interactive HTML, and Typed JSON through relative paths. That is correct for maintenance and traceability, but moving a `.md` file alone breaks its images and requires the original directory structure.
- Decision: preserve the repository's three-part artifacts and add an on-demand self-contained HTML export. Markdown and Viewer-native PNG remain sources of truth. Portable output goes to ignored `build/portable-docs/`, is neither committed nor replaced by the initializer, and never rewrites repository links. It retains external links and in-page anchors while turning repository-relative links into text that shows the original path.
- Implementation: added `scripts/docs/export-portable.mjs`, a controlled HTML template, and a Pandoc Lua link filter. `npm run export:portable-docs` discovers every document with a local Markdown image by default and accepts explicit sources after `--`. Local Pandoc 2.12+ parses GFM, images become byte-identical `data:` values, and an output replaces its predecessor atomically only after checks pass. It currently discovers and exports Architecture Overview, Cross-Machine Preview, Automated CI/CD Setup, and the technical article.
- Security boundary: a pure-Node preflight rejects remote or inline images, path escapes, symlinks, SVG and active formats, raw HTML, empty alt text, an image over 16 MiB, or total document images over 32 MiB. Before writing, it checks the input digest, Pandoc version, and image count; decodes each image to compare MIME and source bytes; and rejects scripts, iframes, base/form, `srcset`, local or unsafe `href/src`, CSS `@import`, and external CSS. Positive and negative fixtures also cover ignoring fences and inline code, emoji-prefix indexing, single- and double-quoted attribute bypasses, byte drift, and stale digests.
- Artifacts and rendering: the four final HTML files were 408,216–2,803,329 bytes and had identical SHA-256 values across two consecutive generations. Chrome checked eight total viewports at 1440×1000 and 390×844: every image decoded, horizontal overflow was absent, and local links and resource requests were both zero. Human review found that the long article's table of contents occupied the first screen; after it became a viewport-limited scrolling region, review passed, and the first embedded Archify diagram was complete on desktop and phone.
- Final gates: `npm run quality` was entirely green. `check:portable-docs` found all four production sources and passed every positive and negative fixture. `npm run check:diagrams` continued to pass `showcase` 9/9, HTML freshness, and native PNG dimension checks for seven Archify sources.
- Data and external services: no npm dependency, user-data collection, telemetry, or runtime third-party service was added. Pandoc is only a local generator; baseline CI and `npm run quality` still require only Node.js 22.

## 2026-08-30 CST / Complete native Archify integration for Claude and Codex

- Gap: the previous round had only the native Claude entry at `.claude/skills/archify/`. Codex rules linked there, but the repository lacked the officially scanned `.agents/skills` entry and therefore did not have complete dual-Agent integration.
- Implementation: retained `.claude/skills/archify/` as the sole vendored implementation, added `.agents/skills/archify/SKILL.md` as the native Codex discovery bridge, and added `agents/openai.yaml` with display name, description, and default `$archify` prompt. The bridge requires the canonical Skill to be read completely, resolves scripts and references relative to that directory, and explicitly inherits the requirement that documentation PNG use Viewer-native **Export → PNG**. It does not copy the 5.9 MB renderer and assets.
- Cross-platform behavior: although Codex supports symlink Skills, Git can degrade directory links into plain text on Windows checkouts without symlink support, so the entry is a regular `SKILL.md`. `docs/contracts/archify.json` moved to schema v2 and records both entry points, Codex UI metadata, and the canonical target. `check:diagrams` rejects a missing entry, symlink or non-regular file, wrong name or target, or missing `$archify` metadata.
- Environment boundary: the active Codex session mounted `.agents` as an empty read-only directory. The two new regular files were therefore written to the Git index with `skip-worktree`, preventing the mount from appearing as a deletion; the gate reads identical bytes from the index only when the worktree path is obscured. After checkout into an isolated temporary directory, `skill-creator` validation passed.
- Verification: `npm run check:diagrams` passed `showcase` 9/9, HTML freshness, native PNG dimensions, offline boundaries, and both Skill entries for all seven diagrams; `npm run quality` was green. The implementation commit fast-forwarded from `dev` into `main`; dev CI run `33318203756` and main run `33318297445` both succeeded.
- Data and external services: no user-data collection, telemetry, runtime third-party service, or MCP dependency was added.

## 2026-08-30 CST / Use Viewer-native Archify PNG for primary documentation images

- Problem: the first integration copied 1440×900 full-page `visual-check` screenshots to `.archify.png`. They included the title bar, guided views, and navigation controls and did not satisfy the requirement to use a natively exported documentation PNG.
- Fix: added `scripts/quality/lib/archify-native-export.mjs`, which invokes the interactive artifact's `Archify.exportMenu.run("png")` in real Chrome. It intercepts only browser download transport to obtain the Blob and leaves the Viewer's native `serializeSvg → 4×/safe scale rasterize → PNG` path unchanged. The receipt must report `format=png` and `canonical=true`, and Blob bytes must match the file.
- Regression protection: `check:diagrams` reads the main SVG viewBox from HTML, reuses the Viewer's 16-million-pixel safe-scale algorithm, and checks PNG IHDR dimensions, so a 1440×900 Viewer screenshot cannot impersonate a primary diagram. Full-page light/dark `visual-check` screenshots remain only under temporary `/tmp` evidence and never enter `docs/diagrams/`.
- Skill synchronization: updated `.claude/skills/archify/SKILL.md` and `references/delivery-contract.md` so future Agents may not use `visual-check` screenshots as documentation PNG, must call `Archify.exportMenu.run("png")`, and must return dimensions, scale, and `canonical=true`. `LOCAL_CHANGES.md` and the machine contract record this third local difference, and the vendored digest was refreshed. `skill-creator`'s `quick_validate.py` passed.
- Result: re-exported all seven documentation PNG files natively at 3× or 4× depending on the canvas, ranging from 3920×2160 to 5988×2608. Human review confirmed that every complete diagram was uncropped and contained no Viewer title bar, section bar, menu, or navigation control. `npm run check:diagrams` passed all seven `showcase` 9/9 checks, HTML freshness, and native PNG dimensions.
- Data and external services: no user-data collection or runtime third-party service was added; export used the project's pinned local Chrome and offline HTML.

## 2026-08-30 CST / Integrate Archify and replace PlantUML

- Decision and evaluation: first extracted and genuinely compiled all seven existing diagrams with local PlantUML 1.2026.1, proving that replacement did not repair syntax. Archify was then evaluated with the same semantics. The medium-density Architecture Overview preserved all six nodes and eight relationships and passed `showcase` 9/9 with 0 errors and 0 warnings. An 11-node dense candidate was rejected because text at the 1440px desktop viewport was only 5.894px, then passed after duplicate wording was condensed, all 14 relationships were retained, and boundaries were corrected. Light/dark themes, four desktop containment sizes, and real screenshots confirmed the visual improvement. Selective Finder, Guided Views, Route Probe, Relationship Lens, and i18n regression reported 31 passing checks and one conditional real-browser skip; after local Chrome was configured, Chinese Finder, Route, Export, and accessibility regression passed 9/9.
- Pinned upstream: as requested, cloned `tt-a1i/archify`. Full-history and normal shallow clones both stalled during remote blob transfer. `gh repo clone ... -- --depth 1 --filter=blob:none --no-checkout` finally retrieved the repository and checked out the core Skill on demand. Commit `4ac500a498267f18bda42b3c82b51edb8f9c1baf`, version `2.16.0-dev.0`, was vendored under MIT at `.claude/skills/archify/`, with `docs/contracts/archify.json` as its machine contract.
- Offline and privacy: the upstream self-contained template still requested Google Fonts, causing Chrome `loadEventFired` timeouts without network and third-party requests when a reader opened a local diagram. The local change replaced remote fonts with the system font stack and disabled automatic update checks in the Skill. `LOCAL_CHANGES.md` records the change and the gate checks the offline boundary. No user-data collection, telemetry, or runtime third-party service was added.
- Toolchain: added `scripts/quality/lib/archify.mjs` and `review-diagrams.mjs`, and rewrote `check-diagrams.mjs` and `render-diagrams.mjs`. `check:diagrams` atomically delivers each diagram to a temporary directory, requires 9/9, compares the result byte-for-byte with committed HTML, and checks PNG and remote resources. `gen:diagrams` atomically refreshes HTML. `review:diagrams` runs four desktop and light/dark Chrome checks in a temporary directory and refreshes only repository previews after success. The CI `diagrams` job now uses Node 22 plus vendored Archify and no Java/JAR.
- Documentation application: migrated Architecture Overview, Cross-Machine Preview, Automated CI/CD Setup, and four technical-article diagrams into seven Typed JSON / interactive HTML / PNG sets: three `architecture`, three workflow schema v2, and one `sequence`. Removed all Markdown PlantUML blocks, old SVG files, the project-level PlantUML Skill, and the shared compilation library. Documentation, Claude/Codex rules, README, scaffold guide, and quality gates now use the Archify three-artifact contract.
- Visual acceptance: every diagram passed `showcase` 9/9 with 0 errors and 0 warnings and had no horizontal or vertical overflow at 1440×900, 1600×1000, 1920×1080, and 2048×1320. Humans inspected light/dark screenshots for the smallest and largest viewports. Architecture Overview, Evolution, Four Layers, and Collaborative Sequence each needed one visual correction; Task Loop, Reuse Flow, and CI/CD Flow needed none.
- Quality fix: ordinary `token = functionCall(...)` expressions in vendored/runtime HTML triggered a false positive in the original secret scanner's unquoted-assignment rule. The matcher was narrowed so a value must end at the line end or a comment, preserving env, shell, and configuration literals. Two expected matches and two source expressions that must not match were verified, and `npm run check:secrets` passed.
- Final verification: `npm run check:diagrams` passed `showcase` 9/9, HTML byte freshness, PNG, and offline boundaries for seven sources. `npm run quality` was green across JavaScript syntax, Markdown indexes and links, contracts, secrets, static site, and CI/CD fixtures. CI-pinned actionlint v1.7.12 was also downloaded to a temporary directory; its SHA-256 matched `8aca8d...a3d8`, and repository workflows plus positive/negative fixtures passed the real binary.
- Branch and remaining work: all changes were on local `dev`. The repository previously had no local or remote `dev`, so only a local branch was created and nothing was pushed automatically. The pinned upstream was the `main` development version at the user's requested clone time. Future upgrades must be explicit, reviewed, and replay all three local changes.

## 2026-07-30 CST / Technical article about the AI coding scaffold

- Completed: added the [technical article](sharing/ai-coding-scaffold.md) for publication on a personal site, explaining the design motivation, four-layer foundation, task loop, project-reuse flow, project facts requiring adaptation, and five evolution directions as foundation models improve. Added four PlantUML diagram narratives for layers, task loop, reuse, and evolution, and clearly separated implemented capabilities, placeholders, and capabilities that still required remote acceptance in a real greenfield project.
- Verification: all four diagrams passed extract → per-diagram compile → write-back → full recompile and produced nonempty PNG/SVG. Standard `check:diagrams` compiled all seven PlantUML blocks, and `npm run quality` was green across script syntax, Markdown links and indexes, contracts, secrets, static site, CI/CD gates, and fixtures.
- Data and external services: no user-data collection or runtime third-party service was added.

## 2026-07-26 CST / Automated CI/CD setup, second increment implemented locally

- Independent actionlint gate: `.github/workflows/ci.yml` gained an Ubuntu `workflow-lint` job that pins actionlint v1.7.12, verifies the archive SHA-256, and confirms ShellCheck availability. A thin `npm run check:workflows` wrapper fails for a missing external binary, forbidden bypass flags, or any actionlint finding. Positive and negative fixtures also give the real actionlint generated Release Please and boolean-`dry_run` deployment workflows.
- Ledger-driven Release Please: pinned the `googleapis/release-please-action` v5.0.0 commit SHA; supported `node` and `simple` with established version-source mappings; generated workflow and config; and created a manifest only on initial bootstrap. It supports default `GITHUB_TOKEN` or a provenance-registered PAT secret and does not claim GitHub App support. The uninitialized scaffold root has no product-version source, so it commits no active Release workflow, config, or manifest.
- Lifecycle and failure boundaries: reject colliding hand-written files, symlinks, config without provable ownership, stale managed artifacts, and missing manifest state. Multi-file writes use staging, backup, atomic replacement, and rollback on failure. Regression covers version sources, package owners, extra-files, SemVer, Git branches, cross-platform workflow filenames, and the restricted config schema; package overrides cannot hide invalid root config. Deployment `dry_run` guards use boolean semantics. Every `kind: deploy` step is explicitly a real publish or safe preparation step, with at least one protected publish step. Global scanning handles multiline YAML scalars, double-quoted Unicode escapes, and `on` block scalars and rejects aliases, anchors, explicit mapping keys, and explicit tag keys that could hide `continue-on-error` or `pull_request_target`.
- zizmor evaluation: an offline regular-persona scan with the official verified-SHA-256 v1.28.0 binary reported eight findings, one suppressed. All seven visible high findings came from official GitHub Actions using v5 tags. Since v1.20.0 it requires SHA pins for all Actions including `actions/*`, unlike the current policy that allows version tags for official Actions and requires 40-character SHA pins for third-party Actions. No permanent or non-blocking fake gate was added; reconsider it after choosing universal pins and automated updates.
- Verification: `npm run quality` was green; real actionlint passed repository workflows and persistent fixtures; `npm run gen:cicd` explicitly skipped at a scaffold root without a ledger and generated no product Release configuration. Temporary branches, a Release PR, tag, and GitHub Release in a real greenfield project remain unexecuted and require remote acceptance after confirming version sources, credential mode, and cadence.
- Data and external services: no user-data collection or new runtime third-party service was enabled. CI downloads and verifies the official actionlint archive; a target project uses Release Please only after its ledger enables it.

## 2026-07-26 CST / Automated CI/CD setup, first increment

- Background: the scaffold previously had only CI content-quality gates, with no CD and no reminder when a greenfield project gained source code. The requested scope was "remind + complete setup automatically + adapt across gcc/C/C++/Python/TypeScript/HTML × Pages/Cloudflare/Vercel/containers/package publication/self-hosting," adapted on demand rather than stored here as ready-made files.
- Design: added [Automated CI/CD Setup](architecture/cicd-autosetup.md). It separates structure and safety skeleton, encoded in the renderer and independent of the stack, from toolchain and commands, which vary per project and come from detected facts plus user decisions stored in JSON. The repository has no finished workflow template. `docs/contracts/cicd-answers.json` is the source of truth and YAML is an artifact, avoiding the conflict between Node 22's lack of a built-in YAML parser and the dependency-free promise.
- First-increment implementation: `scripts/cicd/probe.mjs` detects local signals, eight remote read-only endpoints, and token scopes and exits nonzero on blockers. `scripts/cicd/render.mjs` contains the renderer and roughly 100-line restricted YAML serializer, encoding minimal permissions, 40-character third-party Action SHAs, `${{ secrets.X }}` syntax, explicit `shell:`, no `pull_request_target`, no `continue-on-error: true`, deployment dry-run gates, and `cancel-in-progress: false` for deployment. `scripts/quality/check-cicd.mjs` is part of `quality`, applies safety rules to all workflows, and detects managed drift by rerendering and comparing bytes. Three reminder layers cover the optional `init.mjs` section and todo in `docs/architecture/open-decisions.md`, `.claude/rules/cicd-workflow.md` and `codex-rules/rules/cicd-workflow.md` plus their routing tables, and the independent `.claude/hooks/cicd-reminder.py`; `.claude/skills/setup-cicd/SKILL.md` defines the nine-step loop.
- Evidence: a real detector run found that local token scopes were `gist, read:org, repo` without `workflow`, which would reject a workflow-file push. Renderer fixtures covered a valid C++ CMake matrix plus Pages deployment and eight rejected violations, with generated YAML parsed successfully by PyYAML. Five gate failures—drift, unregistered secret, missing rollback, lost ledger, and managed residue—reported correctly. Five hook cases—uninitialized, should remind, same-day deduplication, existing ledger, and non-Write/Edit—behaved as expected. Fresh-copy `init.mjs` tests covered y/N and idempotent todo recording.
- Two real fixture-discovered bugs were fixed: combining deployment `if` with a user condition placed `&&` outside `${{ }}`, producing an invalid expression; and the original scanner checked only top-level strings in nested `env:` and `with:`, missing an entire invalid secret-reference case.
- Scope: the whole task was large; dividing it made the first increment appropriate. Three items were removed: a block library that would decay in infrequent solo use even though the ledger already preserves knowledge; unselected staging/production layers; and deep zizmor auditing with limited value in the first increment because SHA policy and the `pull_request_target` ban were already encoded.
- Remaining: the second increment had not started—separate actionlint job and `npm run check:workflows`, Release Please integration after deciding a version source, and optional zizmor evaluation. The complete path also still required one real greenfield "make it green + remote apply" run after resolving the missing `workflow` scope.

## 2026-07-15 22:44 CST / Reduce CLAUDE.md to a pure import and remove duplicated state

- Completed: removed Claude Code-specific configuration, Codex-side wording for imported rules, and rule-priority sections from CLAUDE.md, leaving the single-source statement and `@AGENTS.md` import; it fell from 32 to six lines. `.claude/rules/`, `.claude/skills/`, and `.claude/hooks/` are injected automatically at startup, so the CLAUDE.md inventory was a handwritten duplicate. Commit `a7879aa` omitting `sync-shared-rules` was the inevitable result. Removing duplicate state was preferable to a gate that validates it, so the proposed `check:claude-md` was withdrawn.
- Verification against official memory documentation: CLAUDE.md loads at startup; `@` imports expand with the referring file up to four levels; `.claude/rules/*.md` is recursively discovered and loaded at startup with the same priority as `.claude/CLAUDE.md`; user `~/.claude/rules/` loads before project rules. Therefore, omitting `.claude/` from `docs/README.md` does not affect Claude loading—the index would benefit only human readers, and the user chose not to add it.
- User decision: remove both the `apply_patch` compatibility note and `.claude/rules/` priority chain without changing AGENTS.md. Known cost: Claude Code still reads the instruction to use `apply_patch`, which it cannot execute, and conflicts between `.claude/rules/` and `codex-rules/rules/` lack an arbitration rule and may fall back to choosing the more conservative side.
- Completed: removed a fabricated reference in `docs/architecture/dev-workflow.md:35` claiming CLAUDE.md documented a separate temporary-preview port. `git log -S'port' -- CLAUDE.md` showed it never did. The stale reference survived because `check:docs` validates only link resolution, not whether a claim is true.
- Verification: `npm run quality` was green and `check:docs` confirmed `./AGENTS.md` resolves.
- Remaining: none.

## 2026-07-15 21:18 CST / Refine Agent rules and reduce context noise

- Completed: reduced root `AGENTS.md` to always-applicable project boundaries and the shortest work loop; turned `codex-rules/global-AGENTS.md` into a task-based routing table; removed repeated startup, uncertainty, docs-first, verification, language, safety, and PlantUML instructions.
- Completed: moved implementation details for quality commands, CI, and PlantUML into on-demand `docs/architecture/quality-gates.md`; updated `docs/README.md`; refined all Codex topic rules and known issues; and fixed a stale `check-diagrams.mjs` comment about the deleted SVG freshness script.
- Measurement: `AGENTS.md + codex-rules/` fell from 463 lines and 35,648 bytes to 235 lines and 14,992 bytes, reductions of about 49% and 58%; root `AGENTS.md` fell about 75%, from 12,411 to 3,100 bytes.
- Completed: synchronized `CLAUDE.md`, which imports the same rules through `@AGENTS.md` and whose premise changed. Added Codex-specific wording for imports that maps `apply_patch` to Claude Code Edit/Write because the original instruction had been Codex-scoped but became unconditional when moved into general Editing and Verification.
- Completed: added rule priority to `CLAUDE.md`, placing `.claude/rules/` beside `codex-rules/rules/` in the chain established by `global-AGENTS.md`. Removed "take the more conservative side" because it contradicted the same-level rule to ask the user and its own statement not to choose automatically.
- Completed: recorded the previously omitted `sync-shared-rules` Skill from `a7879aa`.
- Verification: `git diff --check` passed; `npm run quality` passed script syntax, Markdown, contracts, secrets, and static site. It remained green after the CLAUDE.md changes, and `check:docs` resolved the three new links.
- Remaining: neither `docs/README.md` nor the AGENTS.md truth-source inventory mentioned `CLAUDE.md` or `.claude/`, so the Claude Code layer was discoverable only from CLAUDE.md itself. Whether to restore an index remained undecided because it conflicted with keeping AGENTS.md limited to cross-Agent consensus.

## 2026-07-14 13:15 CST / Explicitly forbid Codex from guessing user intent

- Completed: root `AGENTS.md`, `codex-rules/global-AGENTS.md`, `codex-rules/rules/codex-workflow.md`, and `codex-rules/rules/language.md` now state that Codex must not guess or fill in unstated intent, preferences, priorities, acceptance criteria, business facts, or authorization. It first verifies discoverable facts; if uncertainty remains or only the user can choose, it states facts, options, and effects and asks for a decision. Nothing dependent on that decision is implemented before confirmation, and "default," "more conservative," or an overridable guess cannot replace consent.
- Completed: corrected this file's previous recommendation to record "a decision I made for you (can be overridden)." Historical entries remain, but choosing first and asking for rejection later is disallowed from this entry onward.
- Remaining: none.

## 2026-07-07 / CI maintenance: upgrade GitHub Actions to v5

- Completed: upgraded all five uses of `actions/checkout`, `actions/setup-node`, and `actions/setup-java` in `.github/workflows/ci.yml` from `@v4` to `@v5`, removing the "Node.js 20 is deprecated" CI warning because their internal runtime moved from node20 to node24. Inputs such as `node-version`, `distribution`, and `java-version` are unchanged in v5. This was warning cleanup only and did not affect gate outcomes.
- Remaining: none.

## 2026-07-07 / Definitive CI fix: retire byte-level SVG freshness and retain compile validation

- Background: after the previous entry aligned CI's PlantUML version to 1.2026.1 and pushed, CI **still failed** at the same step. SVG inspection showed values such as `textLength="41.9998"` and canvas `width/height` derived from text layout, all produced by **JVM AWT font metrics**. Local and CI environments used Temurin 21 but different installed fonts, so even the same PlantUML version emitted different SVG bytes. The conclusion was that `check:diagrams:fresh` byte equality cannot be stable across machines. Version alignment was necessary but insufficient, and the gate's premise—same version means same bytes—was wrong.
- Decision, confirmed with the user as "do not compare SVG; validate PlantUML source": **retire the SVG freshness gate**. Changes: (1) remove "Check rendered SVGs are up to date" from the `diagrams` job in `.github/workflows/ci.yml`, rename the job "Diagram compile check," and retain only `check:diagrams`; keep PlantUML 1.2026.1 because compilation still uses it and it matches development. (2) Remove `--check` comparison from `scripts/quality/render-diagrams.mjs`, leaving a pure SVG generator with a comment explaining why freshness is not checked. (3) Remove `check:diagrams:fresh` from `package.json` while retaining `gen:diagrams`. (4) Update descriptions in `AGENTS.md`, `.claude/rules/markdown-diagrams.md`, and `codex-rules/rules/markdown-docs.md`; rewrite the old version-mismatch entry in `codex-rules/known-issues.md` with the final conclusion and investigation lesson.
- Source-of-truth boundary afterward: PlantUML inside Markdown is the only diagram source, and `check:diagrams` proves it compiles. SVG under `docs/diagrams/` exists for GitHub and other platforms that do not render embedded PlantUML. After changing a source, run `gen:diagrams` locally and commit the result; CI no longer compares its bytes.
- Verification: all five local `npm run quality` gates were green; `PUML_JAR=…1.2026.1.jar npm run check:diagrams` compiled both blocks. The CI result would be confirmed after pushing the commit associated with this entry.
- Correction: the previous entry's version-mismatch "root fix" was incomplete. Retiring the byte gate in this entry was the true root fix. Both entries remain to preserve the complete investigation.

## 2026-07-07 / Fix CI: diagram freshness failed because PlantUML versions differed

- Symptom: after pushing four commits that were ahead locally to `origin/main`, CI ran for the first time with PlantUML diagrams present. The `diagrams` job's "Check rendered SVGs are up to date" step, `check:diagrams:fresh`, reported that `dev-workflow-loop.svg` and `architecture-overview.svg` differed byte-for-byte from output rendered by the pinned version. Ubuntu/Windows Quality gates and compile-only `check:diagrams` passed; only byte comparison failed.
- Root cause, unrelated to the license or bilingual change: `check:diagrams:fresh` compared regenerated and committed SVG bytes, but CI pinned 1.2024.7 while development used 1.2026.1, which `.claude/rules/markdown-diagrams.md` also specified. The version string in the SVG header guaranteed different bytes. In addition, sources changed after the SVG commit without rerunning `gen:diagrams`, so the images were stale; rerendering locally with 1.2026.1 also differed and proved the dual cause. These diagram commits had never been pushed, so CI had never exercised the freshness gate with diagrams and the issue remained hidden.
- Fix, intended as a root fix rather than a workaround: align `.github/workflows/ci.yml` `PUML_VERSION` from 1.2024.7 to 1.2026.1 and replace `PUML_SHA256` with the official release JAR digest `89c1…3092`. The official JAR URL was downloaded with curl, and its SHA-256 matched `~/work/envcfg/plantuml-1.2026.1.jar` byte-for-byte. Rerender both SVG files through `npm run gen:diagrams` with 1.2026.1. With the same JAR, local `check:diagrams` compiled two blocks and `check:diagrams:fresh` reported both SVGs current. The issue and fix were added to `codex-rules/known-issues.md`.
- Decision I made for you (can be overridden): upgrade CI to **1.2026.1**, matching the actual local environment, rather than downgrade SVG output to 1.2024.7. Both the development environment and `.claude` diagram rules used 1.2026.1; making the CI validator follow the generator appeared necessary to prevent the next local `gen:diagrams` from breaking CI again.
- Remaining: CI still had a non-failing **warning** that Node.js 20 was deprecated because `actions/checkout@v4`, `setup-node@v4`, and `setup-java@v4` used node20 internally. Actions were not changed in this task to avoid unrelated work and could be upgraded to v5 separately. If freshness remained red after push because local and CI Java/font environments produced different bytes, byte comparison would be proven cross-environment fragile and would require a separate structural-equivalence design.

## 2026-07-07 / Make commit messages bilingual with English first and switch license to Apache-2.0

- Completed two scaffold-convention changes:
  1. **Bilingual commit messages with English first:** changed the subject format to `<type>(<scope>): <English subject> / <Chinese subject>`, separated by ` / `. Tightened `.githooks/commit-msg` from `...: .+$` to `...: .*[A-Za-z].* / .+$`, requiring an English part with at least one ASCII letter first, the separator, and a nonempty second part. Git-generated merge, revert, Reapply, fixup!, squash!, and amend! subjects remained exempt. Eleven end-to-end cases with the real hook allowed a valid bilingual subject, `A/B` and ` / ` inside the English segment, and three exemption prefixes; they rejected Chinese-only, English-only, empty English part, missing scope, and missing type. Updated shared `AGENTS.md`, `codex-rules/rules/git-workflow.md`, `.claude/rules/git-workflow.md`, `.claude/rules/language.md`, `CONTRIBUTING.md`, `SCAFFOLD.md`, and `docs/architecture/stack-recipes/typescript.md`, whose Commitlint alternative did not enforce bilingual structure.
  2. **MIT → Apache License 2.0:** replaced `LICENSE` with the official Apache 2.0 text retrieved from the raw `.txt` at apache.org, changing only the appendix copyright line to `Copyright __COPYRIGHT_YEAR__ __COPYRIGHT_HOLDER__`. The existing `init.mjs` placeholder path remained valid. Added `"license": "Apache-2.0"` to `package.json` and a License section to `README.md`.
- Verification: all five `npm run quality` gates were green: check:js, check:docs, check:contracts, check:secrets, and check:site.
- Decisions I made for you (can be overridden): (a) put both languages on **one subject line** separated by ` / ` rather than an English subject and Chinese body, because "English first" seemed to refer most directly to the visible subject; the hook and docs could change if an English subject plus Chinese body was preferred. (b) Validate only **structure**—English first, delimiter, nonempty second part—not literal CJK, which is brittle in dependency-free cross-platform shell and consistent with the hook validating structure rather than semantics; humans verify the second part is Chinese. (c) Do not add an empty `NOTICE`: Apache-2.0 does not require one, and an empty file creates propagation obligations without value; add it when third-party attribution exists.
- Remaining: the 2026-07-06 historical entry in `docs/progress.md` retained the older `<type>(<scope>): <subject>` format because it accurately described behavior at that time.

## 2026-07-06 / Final review: fix three small defects in the recipe-adoption commit

- Completed: performed final review of the commit that adopted recipes from three external projects. All gates were green, hook modes were 100755, and spot checks matched real DocRestore-pro, Augur_Maestro, and Narrative_Maestro configuration. Fixed three issues: (1) added missing `import { defineConfig } from "vitest/config"` lines to both Vitest snippets in `docs/architecture/stack-recipes/typescript.md`, fulfilling the copy-and-paste promise. (2) Extended `.githooks/commit-msg` exemptions from merge/revert to the Git-generated `Reapply "` and `rebase --autosquash` prefixes `fixup!`, `squash!`, and `amend!`; synchronized `codex-rules/rules/git-workflow.md` and retested nine end-to-end cases in a temporary repository, allowing valid/autosquash/Reapply/merge/revert while still rejecting arbitrary or scope-less subjects. (3) Replaced a stale scope enum copied into `.claude/rules/git-workflow.md`—`core|agents|memory|skills|tools|tui|meeting`, which omitted this repository's `scaffold`—with a rule that scopes follow project modules and the hook requires a scope without enforcing an enum, matching actual behavior.
- Remaining: none.

## 2026-07-06 / Adopt reusable scaffold recipes from three external projects

- Completed: studied engineering infrastructure in DocRestore-pro, Augur_Maestro, and Narrative_Maestro, including Claude rules/hooks, Codex rules, pre-commit, CI, and documentation organization, then implemented general recipes that were not already present: (1) added `codex-rules/rules/issue-workflow.md`, resolving the lack of any Codex issue-decomposition rule, and indexed it in `codex-rules/global-AGENTS.md` and `AGENTS.md`. (2) Added high-cohesion/low-coupling module design and single-source docs specs to the Evolution Principles section of `docs/architecture/overview.md`. (3) Added active/archive splitting for long documents to `codex-rules/rules/markdown-docs.md`. (4) Added Find a Document by Question to `docs/README.md`. (5) Added a design-document field to the PR template and a "decision I made for you (can be overridden)" note to `docs/progress.md` entry format. (6) Added CI job splitting and migration-ledger evolution guidance to `docs/architecture/open-decisions.md`. (7) Added optional `docs/architecture/stack-recipes/python.md`, `typescript.md`, and `migration-ledger-check.md`, pairing concrete configuration with the existing `.claude/rules/python-coding-rules.md` and `.claude/rules/typescript-coding-rules.md` prose rules. (8) Added dependency-free `.githooks/commit-msg` to enforce the previously prose-only `<type>(<scope>): <subject>` format and tested valid, invalid, merge, revert, and empty-message cases in a real temporary Git repository. (9) Switched CLAUDE.md to native `@AGENTS.md` import, merging commands, directory responsibilities, and documentation-consistency gate details into AGENTS.md as the sole source while leaving only Claude-specific `.claude/` notes in CLAUDE.md.
- Existing bug found and fixed: `.githooks/pre-commit` was mode `100644` in the Git index, so after `core.hooksPath` it was silently ignored and had never run. Fixed with `chmod +x` plus `git add --chmod=+x`, recorded it in `codex-rules/known-issues.md`, and made the new commit-msg hook executable as well. `npm run quality` ran after every incremental change.
- Decision I made for you (can be overridden): following the direction accepted through AskUserQuestion, the migration consistency gate became an independent reference-script document rather than joining `check-contracts.mjs`. The repository had no migration tool, and hard-coding it into the core gate would prebuild generality for zero consumers. The commit-msg type enum reused the six types already in `git-workflow.md` without expanding it.
- Remaining: no new item. The existing difference in layering and content between same-topic `.claude/rules/` and `codex-rules/rules/` remained unchanged by prior user decision and was not introduced by this task.

## 2026-07-06 / Automate PlantUML artifacts and add a freshness gate

- Completed the automation gap left by the previous entry. Added `scripts/quality/lib/plantuml.mjs` to extract PlantUML blocks, associate the immediately following `![](path.svg)` reference, and compile them for shared use. `scripts/quality/render-diagrams.mjs` exposed `npm run gen:diagrams`, which compiles and overwrites referenced SVG files so source changes no longer need manual `java -jar`, and `npm run check:diagrams:fresh`, whose read-only `--check` mode compared committed and recompiled SVG bytes, reported the exact stale file, and suggested `gen:diagrams`. `check-diagrams.mjs` reused extraction/compilation but still checked only exit status, avoiding cross-version byte false positives.
- CI and artifacts: the `diagrams` job gained a freshness step using the pinned JAR. Both `docs/diagrams/architecture-overview.svg` and `docs/diagrams/dev-workflow-loop.svg` were regenerated with CI-pinned PlantUML 1.2024.7 instead of the developer's local 1.2026.1, which had embedded a different version header, preventing the new gate from immediately flagging old artifacts. Local verification with both versions confirmed that `check:diagrams` was version-independent and that the freshness check passed with the pinned version. Claude/Codex diagram rules and CLAUDE.md were updated.
- Remaining: no new item. The existing same-topic differences between `.claude/rules/` and `codex-rules/rules/` remained outside scope.

## 2026-07-06 / Standardize on PlantUML and add a CI compilation gate

- Completed: resolved the Mermaid/PlantUML conflict after the user selected PlantUML everywhere. `codex-rules/rules/markdown-docs.md` now required PlantUML and prohibited Mermaid, ASCII art, screenshots, and binary pasted images, linking to detailed `.claude/rules/markdown-diagrams.md`. Replaced the Mermaid flowchart example in `docs/architecture/overview.md` with a PlantUML component diagram. Added `scripts/quality/check-diagrams.mjs`, separate from `npm run quality`, which scans repository Markdown for fenced PlantUML and genuinely compiles it through `java -jar $PUML_JAR`; it skips when there are no diagrams but fails rather than silently skipping when diagrams exist and `PUML_JAR` is unset. Added an Ubuntu-only `diagrams` CI job that downloads the official PlantUML 1.2024.7 release JAR, verifies its SHA-256, and runs the check. `docs/architecture/overview.md` and `docs/architecture/dev-workflow.md` gained rendered `docs/diagrams/*.svg` references. Both diagrams compiled with personal 1.2026.1 and CI-pinned 1.2024.7, and `npm run quality` plus `check:diagrams` passed.
- Remaining: compilation proved syntax only and did not verify that rendered images under `docs/diagrams/` matched current source, so sources still required manual rendering and SVG commits. Existing content differences in same-topic Claude/Codex rules such as tool-failure and git-workflow remained outside scope.

## 2026-07-06 / Add project-level `.claude/` configuration for rules, Skills, and hooks

- Completed: copied the user's global `~/.claude/` rules (10 topic files), Skills (`plantuml-in-markdown` and `view-gel-image`), and hooks (`pre-edit-validate.py`, `post-edit-safety.py`, and their `settings.json` registration) into a project-level `.claude/` copy. Claude Code discovers it natively alongside the global copy without interference. Removed every hard-coded personal absolute path: PlantUML now requires explicit `PUML_JAR` and fails clearly when absent instead of using a wrong default; hook/rule references changed from `~/.claude/...` to project `.claude/...`; hook commands in `settings.json` use `${CLAUDE_PROJECT_DIR}`. `.claude/settings.json` retains only hook registration and no personal model, effort, permission, or other UX preference. `.gitignore` gained `.claude/settings.local.json`; CLAUDE.md explains the directory and its relationship to Codex rules; `npm run quality` passed.
- Remaining: `.claude/rules/markdown-diagrams.md` required PlantUML and prohibited Mermaid, while existing `codex-rules/rules/markdown-docs.md` defaulted to Mermaid. Both rule sets remained separate and contradictory until the user chose a final diagram tool. Same-topic differences such as tool-failure and git-workflow also remained, per the user's request to keep both layers separate.

## 2026-07-05 / Scaffold self-review and repair

- Completed: after multi-Agent review, added `.gitattributes` to normalize line endings and fix Linux `bad interpreter` errors in `pre-commit` and `sync.sh` caused by CRLF. Expanded secret scanning to `.sh`, `.ps1`, `.py`, and extensionless files and strengthened patterns for unquoted assignments, credentials embedded in URLs, `client_secret`, and similar forms. Made contract matching use Unicode word boundaries and handle invalid regexes. Made the Markdown gate ignore fences and inline code and determine index coverage from actual link targets. Expanded `check:js` to `init.mjs` and `lib/files.mjs`. Parameterized `LICENSE`; made `init` fill year/owner and support rerunning only preview setup. Removed source-project identity leaks from CI and content rules; added `dev` and a Windows matrix to CI. Prevented `preview.sh` from taking over an unrelated process occupying its port. Made `restart-remote.ps1` use `REMOTE_USER` and `SSH_KEY_NAME` and validate branch/path injection. Added `CONTRIBUTING.md` and placeholder `public/index.html`.
- Remaining: CODEOWNERS ownership cannot be verified offline because it depends on GitHub settings. Test framework and dependency/lockfile policy remain open until the technology stack is chosen; see [Open Decisions](architecture/open-decisions.md).
