name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - dev

permissions:
  contents: read

jobs:
  quality:
    name: Quality gates
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Quality gates
        run: npm run quality
        shell: bash

  diagrams:
    name: Archify + PlantUML diagram checks
    runs-on: ubuntu-latest
    env:
      PUML_VERSION: "1.2026.1"
      PUML_SHA256: "89c116168a2a0f7cf5292e11617ba22abd743f891914f1fec5bc9c7d257b3092"

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Download pinned PlantUML
        run: |
          set -euo pipefail
          jar_path="${RUNNER_TEMP}/plantuml.jar"
          curl --fail --silent --show-error --location \
            --output "${jar_path}" \
            "https://github.com/plantuml/plantuml/releases/download/v${PUML_VERSION}/plantuml-${PUML_VERSION}.jar"
          printf '%s  %s\n' "${PUML_SHA256}" "${jar_path}" | sha256sum --check -
          printf 'PUML_JAR=%s\n' "${jar_path}" >> "${GITHUB_ENV}"
        shell: bash

      - name: Check Archify and PlantUML diagrams
        run: npm run check:diagrams
        shell: bash

  workflow-lint:
    name: Workflow lint
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      ACTIONLINT_VERSION: "1.7.12"
      ACTIONLINT_SHA256: "8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8"

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: "22"

      - name: Verify ShellCheck is available
        run: shellcheck --version
        shell: bash

      - name: Download pinned actionlint
        run: |
          set -euo pipefail
          archive="${RUNNER_TEMP}/actionlint.tar.gz"
          curl --fail --silent --show-error --location \
            --output "${archive}" \
            "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz"
          printf '%s  %s\n' "${ACTIONLINT_SHA256}" "${archive}" | sha256sum --check -
          tar -xzf "${archive}" -C "${RUNNER_TEMP}" actionlint
          chmod +x "${RUNNER_TEMP}/actionlint"
          "${RUNNER_TEMP}/actionlint" -version
          printf 'ACTIONLINT_BIN=%s/actionlint\n' "${RUNNER_TEMP}" >> "${GITHUB_ENV}"
        shell: bash

      - name: Check workflows
        run: npm run check:workflows
        shell: bash

      - name: Check actionlint fixtures
        run: npm run check:workflows:fixtures
        shell: bash
